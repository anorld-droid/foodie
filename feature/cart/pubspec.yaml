name: cart
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  intl_phone_number_input: ^0.7.2
  dropdown_search: ^5.0.5
  location: ^4.4.0

  # Modules
  common:
    path: ../../core/common
  model:
    path: ../../core/model
  domain:
    path: ../../core/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
