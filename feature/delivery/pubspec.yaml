name: delivery
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_polyline_points: ^1.0.0
  google_maps_flutter: ^2.2.5
  location: ^4.4.0
  get: ^4.6.5

  model:
    path: ../../core/model
  common:
    path: ../../core/common
  domain:
    path: ../../core/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
