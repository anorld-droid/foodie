name: instore
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.5
  qr_code_scanner: ^1.0.1

  model:
    path: ../../core/model
  common:
    path: ../../core/common
  domain:
    path: ../../core/domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
